
import "env"
    void log(int n)
    void log(float n)
    void log(char n)
    void log(string n)

int[] a = []
a.push('a')
a.push('c')

log("Popped: " + (char)a.pop() + " -- " + a.length)
log("Popped: " + (char)a.pop() + " -- " + a.length)